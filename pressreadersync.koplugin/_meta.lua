local _ = require("gettext")
return {
    fullname = _("PressReader Sync"),
    description = _([[Unofficial PressReader integration: browse authorised newspaper and magazine files from a PressReader Sync Bridge, download them, and open them in KOReader.]]),
}
